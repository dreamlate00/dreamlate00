在for循环完成数组元素的处理

```python
# 定义一个原始数组
original_array = [1, 2, 3, 4, 5]

# 使用列表推导式生成新数组
new_array = [element * 2 for element in original_array]

# 输出结果
print("原始数组为：", original_array)
print("生成的新数组为：", new_array)
```

