判断文件夹是否存在并创建

```python
import os

folder_path = '/path/to/folder'  # 替换为你想要判断和创建的文件夹路径

if not os.path.exists(folder_path):
    os.makedirs(folder_path)
    print(f"文件夹 {folder_path} 创建成功")
else:
    print(f"文件夹 {folder_path} 已经存在")
```

